db_port: 3306
db_ip: 127.0.0.1
db_database: tmgg-sys
db_username: root
db_password: 123456

spring:
  profiles:
    active: default

  application:
    name: 'template'
  jpa:
    show-sql: false

sys:
  cache-dir: /cache/project-cache

logging:
  level:
    io.tmgg: info
  file:
    path: ./logs/

